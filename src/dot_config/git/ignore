# Global gitignore — applies to every repo
# Reference: git config --global core.excludesFile (chezmoi/git already auto-detects this XDG path)

# Claude Code per-project local settings (machine-specific permissions)
**/.claude/settings.local.json

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Editors
.vscode/
.idea/
*.iml
*.swp
*.swo
*~
.netrwhist

# mise — machine-local runtime/env overrides (never committed)
mise.local.toml
.mise.local.toml

# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint
DerivedData/
*.hmap
*.ipa
*.dSYM.zip

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (only when used)
Pods/

# fastlane artifacts
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/

# Common secret files
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
