# ~/.ssh/config — managed by chezmoi (mode 0600 via the `private_` prefix).

Host *
    # 1Password SSH agent for all keys.
    IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"

    AddKeysToAgent yes
    UseKeychain yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
    HashKnownHosts yes
