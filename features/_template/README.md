# <Human title>

One or two sentences: what this feature is, and the thing about it that is not
obvious from the code.

## Verbs

- `chez <verb>` — one line, matching the summary in `core/verbs.sh`.

Delete this section if the feature has no verb.

## How it works

The design, and the reasons behind it. This is the single home for this
feature's prose — not `CLAUDE.md`, not a table cell in `docs/commands.md`.
Write down what a future reader would otherwise have to reconstruct: why a step
is ordered where it is, what breaks if it moves, which failure modes are silent.

## Gotchas

Anything load-bearing that looks incidental.
