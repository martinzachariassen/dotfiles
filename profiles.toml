# Wizard presets. Read only by `dot config --init`; `dot apply` never reads
# this file. See docs/configuration.md.

# Copied into config.toml, never asked: none of it varies by machine. The
# signing key is the PUBLIC half; empty means no signing.
#
# Forked this repo? Edit the three values below first -- the wizard writes
# them as-is, so an unedited fork authors commits as Martin and turns on
# signing with a key whose private half lives only in his 1Password vault.
[user]
name = "Martin Zachariassen"
email = "10747123+martinzachariassen@users.noreply.github.com"
signingkey = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDFG+XYhuxIpAr2LB8Y1AkOocZ+N/YpeDqNYUn/F68OR"

[profiles]
personal = ["git", "ssh", "zsh", "dev-cli", "apps", "cmux", "containers", "macos-defaults", "claude-code", "dotfiles-dev"]
work = ["git", "ssh", "zsh", "dev-cli", "apps", "cmux", "containers", "macos-defaults", "work-apps", "dotfiles-dev"]
minimal = ["git", "ssh", "zsh"]
