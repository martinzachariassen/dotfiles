# jq is named by dev-cli and dotfiles-dev too, on purpose: a module names what it
# needs rather than inheriting it, since modules cannot depend on each other.
# `brew bundle` is idempotent.

cask "claude-code@latest"
brew "jq" # statusline.sh parses its stdin with it
