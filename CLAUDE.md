# CLAUDE.md

Repo-specific guidance for this dotfiles project lives in
[`.github/copilot-instructions.md`](.github/copilot-instructions.md) — a single
source of truth shared by Claude Code and GitHub Copilot. Read it before making
changes here (chezmoi `src/` split, naming conventions, hooks, quality gates).

My personal cross-project defaults (stack, code style, workflow) come from the
global `~/.config/claude/CLAUDE.md` and apply on top of the repo rules above.
